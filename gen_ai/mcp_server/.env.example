# FastMCP server configuration
# Copy this file to .env and adjust as needed.

# Server display name
MCP_NAME=Itinerary MCP Server

# Transport: stdio (default for Claude/Cursor) or http
MCP_TRANSPORT=stdio

# HTTP only (when MCP_TRANSPORT=http)
MCP_HOST=127.0.0.1
MCP_PORT=8000

# Get your key(s) at https://openrouter.ai/keys. Comma-separated keys are used in cyclic rotation to spread rate limits.
# If you see 401 "User not found", the key is invalid or missing—create/regenerate a key at the link above.
OPENROUTER_API_KEY=

# Optional: override default model (e.g. anthropic/claude-3-haiku)
# OPENROUTER_MODEL=arcee-ai/trinity-large-preview:free

