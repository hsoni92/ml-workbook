Welcome back learners. Now let's get
into the main logic for this app, the
quiz generation logic. Now let's
develop the main logic of this
application, right? So we will begin
with the quiz generation logic. Great.
So look at this. I've defined a
function called generate quiz, right,
which will first begin with printing
this sentence, so generating a quiz on
topic. This topic will come from the
user, and this exact topic will be
passed as an input to this function.
Great. Now within the try, I've added
the logic, so I'm defining a variable
to get the response from the LLM, so
client.models. generate content. I will
pass the model name. I will pass the
prompt template with the topic that has
been defined by the user. Right? Now
the important bit.
As output, I'm passing response mine
type equals application JSON, right? So
this will ensure that all the output
from the LLM is in JSON format and JSON
format only, right?
Finally, I will capture this response
and I will extract the text from this
response, and let's store it in this
variable. Great.
Now, what we will do, we will again do
some output parsing using regex, right?
So this is a pattern just to extract
the Json from the output, right?
Now, let's see. What I'm doing is
cleaned Jason string, rejects.s
substitute. Any garbage value that is
being generated by the LLM, I want it
to be replaced with an empty string.
Basically, I want to delete it, right?
So this will ensure consistency in our
output. Great! Finally, I will return.
The clean JSN string in the JSON format.
Great, let me add one space here, OK.
Again, then I have some exception
handling you. Great. So our main quiz
generation logic is ready, let me run
this.
Perfect. We're ready to roll. Now in
the next video we will implement the
main application runner. I hope you
enjoyed this and I'll see you in the
next video. Thank you.
