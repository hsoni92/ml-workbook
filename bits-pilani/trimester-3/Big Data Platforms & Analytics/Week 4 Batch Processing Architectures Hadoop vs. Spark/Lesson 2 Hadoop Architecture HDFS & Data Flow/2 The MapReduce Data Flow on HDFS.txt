Now that we understand how HDFS stores
and protects our data using blocks and
replication. Let's look at how we
actually process that data. In this
video, we'll trace the MapReduce data
flow on HDFS. As shown on the slide,
the journey begins with data locality.
Instead of moving massive amounts of
data to where the code is. Hadoop does
the opposite. It moves the map, which
is your code.
To the data nodes, where the HDFS
blocks already live. This avoids
clogging the network with huge file
transfers. During the map phase, each
mapper reads its assigned HDFS blocks
and processes the records.
But here is a critical point, the
intermediate results generated by the
mappers are not written back to HTFS.
Instead, they are written to the local
disc of the data node. This is done for
efficiency. As these intermediate
results are temporary. And don't need a
heavy overhead of 3x replication. Next
comes the shuffled and sad face. This
is the most intensive network part of
the flow, as we have already discussed
in the earlier modules. The system
moves the intermediate data across the
network to the specific reduced nodes
based on the data keys. Finally, we
reach the reduce phase, the reducer
processes the shuffled data and
produces the final output. Unlike the
intermediate data, this final output is
written back into HTFS. Because it is
going into HTFS, the system immediately
begins the replication process we
discussed in the last video. Ensuring
the final result is durable and fault
tolerant, this cycle. Reading from HDFS,
writing temporary data to local disc
and writing final results back to HTFS
is a core engine of Hadoop. However, as
we as we will see in the next lesson,
all those disc rights come with a
significant performance cost.
Up next, we'll examine the bottlenecks
of this architecture. High IO latency.
Till then, happy learning, and I'll see
you in the next class.
