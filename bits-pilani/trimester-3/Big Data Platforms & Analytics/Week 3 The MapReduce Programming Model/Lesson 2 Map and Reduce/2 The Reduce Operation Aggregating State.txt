In our last video We looked at the map
operation. The phase where we peeled
our raw data and transformed it into
thousands of independent key value
pairs like index.L.1. But having a
billion ones doesn't help a business
make decisions. We need a final total.
This is where the second half of the
processing brain comes in. The reduce
operations. While mapping is about
transforming, reducing is about
aggregating state. It is a stage where
we consolidate those parallel streams
into a single summarised answer. Let's
look at the formal definition. An
aggregation function combines multiple
values, sharing the exact same key. If
you recall our functional programming
roots, this is called a fold operation.
You're folding a long list of items
into one representative value. The
signature for this operation is reduced
with arguments key and list of values.
And it will give you a list of values.
Notice a very important difference here.
Compared to the map phase, the input to
the reducer isn't just one value, it's
a list of values. The framework has
already done the heavy lifting of
gathering every single one associated
with index.html. And handing that
entire list to the reducer. Let's
continue with our URL popularity
example from the slide. Imagine the map
phase produced 10,000 different pairs
of STML and count. The framework groups
them together. Now the reducer receives
one single request. The key is
home.stML, and the value is a list
containing 10,000 ones. The logic
inside the reducer is simple addition.
It iterates through the list, adds all
of them up, and emits one final
definitive pair so it can say home.html.
10,000. We have successfully turned
noise, which is individual clicks. Into
a signal, which is a total wizard
counter. As you can see from the
results section on our slide, the
output of this phase is a much smaller
summarised set of key value pairs. In a
real world web log of 10 terabytes, you
might have billions of log lines. But
by the time the reduce phase is
finished, you might only have a few
megabytes of data, a simple ranked list
of your top 100 URLs. This massive
reduction in size is why model is so
powerful for big data analytics. We
move from overwhelming volume to
actionable insight. To wrap up lesson
two, map transforms raw data into pairs
in parallel. Reduce aggregates those
pairs into a final result. You now
understand two core functions of the
model.
However, there is a big magic trick
happening in the middle. How did the
ones from 1000 different mappers find
their way to the same exact same
reducer? In our next lesson, we'll look
at the logical data flow, where we'll
explore the shuffle and the sort phase.
The bridge that connects map to reduce.
I'll see you in the next lesson.
