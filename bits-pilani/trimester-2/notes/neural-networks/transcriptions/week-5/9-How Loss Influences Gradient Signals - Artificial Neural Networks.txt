Hello everyone. Welcome back to Module 5 of Artificial Neural Networks. In this video,
we will explore how loss functions influence gradient signals. By the end of this video,
you will see how the choice of loss function shapes the gradients that drive learning,
comparing MSE and cross-entropy, and understanding why cross-entropy typically produces stronger,
more useful gradients for classification. Remember the training pipeline? Forward pass,
then loss, then backpropagation, and finally weight update. The loss is not just a measure of error,
it directly shapes the gradients computed during backpropagation. Different losses produce
different gradient magnitudes and directions, and that fundamentally changes how fast and how well
a model learns. Now, let's compare MSE and cross-entropy at a high level. With MSE,
the gradient for a classifier typically looks like error into activation derivative. That activation
derivative for sigmoid or softmax can be very small when outputs are near 0 or 1. With
cross-entropy paired with softmax, the gradient simplifies to something like predicted probability
minus target. There is no extra tiny activation derivative multiplying the error. The practical
implication of this is cross-entropy gradients are often much larger and more direct, especially
when the model is wrong and needs a big correction. Now, let us discuss some common cases.
Consider a model that is unsure. It gives probability of around 0.5 for the correct class. For MSE,
the error term is 0.5 and the activation derivative is 0.25. So, the gradient is 0.125, which is a moderate value.
While for cross-entropy, the gradient is 0.5, which is a stronger corrective signal. Cross-entropy gives a larger push when the model is uncertain. So, it moves weights faster towards the correct behavior. Now, the dramatic case. The model is confidently wrong. Probability is around 0.01 for the true class.
With MSE, the error term is 0.99. With MSE, the error term is 0.99. But the activation derivative at that saturation point is tiny. So, the product, the actual gradient, becomes almost 0.00. With cross-entropy, the gradient is minus 0.99. A very large signal to correct the mistake.
This is the crucial difference. This is the crucial difference. Cross-entropy aggressively punishes confident wrong answers. MSE often does not because of the saturation of the activation derivatives.
That's why MSE can leave a confidently wrong classifier stuck. Now, when the model is confident and correct, both losses produce small gradients. The model doesn't need bit changes. That's appropriate. Small updates fine-tune behavior when performance is already good.
Here's the conceptual plot. Here's the conceptual plot. Horizontal axis is predicted probability, P, and vertical axis is the gradient magnitude. You can see, the cross-entropy curve starts high when P is small and declines smoothly towards 0 as P increases.
The MSE curve peaks somewhere near the middle and is very small at the edges. Cross-entropy provides robust corrections when the model is wrong, while MSE tends to give weak corrections when the model is confidently wrong. Exactly the situation where you most need strong learning signals.
Putting all of this together, cross-entropy produces stronger gradients for wrong predictions, which leads to faster correction. It aligns gradients with probability error directly P minus Y.
MSE introduces an extra multiplier, which is the activation derivative, which can dampen signals via saturation. That's why, empirically, classification models trained with softmax plus cross-entropy,
converge faster and reach better performance than the same models trained with MSE. Now, let us summarize the main points of this video. The loss function affects the gradient you use to update the weights. MSE gradients shrink due to activation saturation. Cross-entropy gives larger, better shaped gradients for classification, especially when the model is wrong. Choosing the right loss is the same model.
is therefore, a design decision that directly impacts learning dynamics. In the next lesson, we'll examine how these gradients behave across depth, why multiplying many local derivatives can cause vanishing or exploding gradients, and how initialization and architectural choices help mitigate those issues. Thank you.
