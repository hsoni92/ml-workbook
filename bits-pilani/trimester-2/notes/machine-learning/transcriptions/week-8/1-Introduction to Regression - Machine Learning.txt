Hello to all of you. My name is Dr. Hemantrathar and we are in the course Machine Learning. We are in the module 8 Regression and today's topic is Introduction to Regression.
So the learning objective of this video are, we are going to first define what is Regression, then we will look into how Regression is different from Classification, then we will also look into the key components of Regression like Features, Target Set and Target Data and Training Set. Then we will look into the Hypothesis function. So let's start right away.
So till now, last few modules we have been learning Supervised Learning. In Supervised Learning, we were actually learning Classification.
right for example we looked into decision tree classifier we looked into rule-based classifier
we looked into canon classifier we looked into inseable classifier and there are few more
classifiers so what is supervised learning if you just quickly want to revise supervised learning
is a branch of machine learning which teaches the model with an example right so in the data
set we are given a labeled data set so data set is a labeled data set where we have x and the corresponding y so we have input features which is my questions and the corresponding labels that are my answers right what we want to do in class supervised learning is to find the underlying pattern or relationship between or mapping between x and y so for a given x right what
would be the value of y right this is what we want to do in supervised learning so we saw that supervised learning is broadly divided into two categories of algorithm classification and regression what we have been doing till now is classification where the output the y is always discrete or categorically in nature for example spam detection whether an email is a spam or no spam cat dog and fish right
for example i have been given n number of images i have to look i have to look i have to build an ai model which looks inside each of these images and find out whether it contains cat dog or fish right for example pass and fail all these are classification problems because the y the number of unique attribute values of y are finite and fixed for example this is 2 this is 3 this is 2 right so y column if i count the unique attribute values
of y it is finite and fixed on the other hand if you look into regression in regression in regression is also a supervised learning algorithm or a task or group of algorithms but the output here is continuous or numerical in nature for example house price prediction now house price can be 100 rupees 101 rupees 102 rupees 103 rupees and so on there can be infinite number of values here
why the value of y the value which you are predicting y is always discrete on the other hand in regression problem the y which you want to predict is essentially continuous in nature in numerical in nature example gold price prediction right today gold price is 1.5 lakh right if i want to predict gold price for tomorrow it can be 1 lakh 51 thousand it tomorrow it might be day after tomorrow it might be 1 lakh 52 thousand and so on so these are continuous values so
house price prediction temperature prediction what would be the temperature prediction what would be the temperature to borrow will it be 30 degree 31 32 29 28 there can be infinite number of values so here given a tabular data assuming i have a tabular data this we have seen again and again right i have x1 x2 and x3 which is essentially my x and the class label y so here we do not have class labels right so here we had two labels in classification we had two
two labels three labels three labels those number of labels are finite in regression the number of labels are infinite right so that is why it is numeric value or continuous value so this can be 100 101 102 103 and so on right student exam score right or stock value stock value can be 1 lakh rupees 1 lakh 2 rupees 2 lakh rupees 5 lakh rupees so this can be infinite number of values so classification and regression
both are supervised learning algorithms both are supervised learning algorithms both are supervised learning algorithms or tasks right in both we are doing some sense of prediction in classification we are predicting a class label the number of class labels are finite discrete in nature in case of regression again i am doing prediction but i can predict infinite number of values so regression is a statistical method at the core of machine learning where we are trying to find relationship
between x and x between x and y so for a given x right so i have to build a function f such that if i give a new x to it it can predict y for me right it can predict y for me right it can predict y for me in this example of house price prediction right this data has been given to me i have this x which is only x which is living area and y which is my price right i have to build a function f such that i can find the mapping
something between x between x and y i can look into the data set this is row number one if the living area is 2104 then the price is 400 dollars right something of that sort if the living area is 1600 the price of the house is 330 and so on so i have to build a mapping a function which maps x to y now in this case x is only one attribute but in regression you can have many attributes as well for example
house price might not only be dependent on the area might also be dependent on the area might also be dependent on the location or maybe story right so location let's talk about location right so if the location is a and area is 2104 what is the price if the location is b and so on or it can be based on many other attributes all these will combine x will
will be part will be essentially x right and i have x1 x2 x3 and so on right so this is how regression would look like in regression as you can see y are numerical values are continuous value so the idea is given a let's suppose a living area size of 5000 can you predict my model can predict what would be the price that is the idea of regression so regression is a prediction engine the goal of regression is to predict a
continuous target value based on x is to predict a continuous target value based on x input features right x can be one or many features right x can be one or many features
now so similar to classification regression also is essentially will be working on this sort of a structure you have x you have y right now you have to break this so this is my original data i have to break this original data into two parts training data and test data
on training data i am going to build the model the regression model the f of x right and i am going to test whether that this f of x is working properly or not right i am going to test it on test data right so training data is essentially collection of historical data for example historical data is this right this which also include y so x and y right x contains the input attributes
and y is the continuous output value or input variables it can be one attribute or many attributes and y is the continuous output value right
y is the continuous output value which can be price or something else right so now the basic idea of regression or learning process of regression is given a training data you have to build a function the hypothesis function fx the mapping function fx right so given x
you have to build a hypothesis function f which maps x to y x is my training data and f is my hypothesis function fx which maps x to y and then once the hypothesis function is ready then i am going to put it for test in testing on testing data so how it would work now let's suppose it is x value is ranging from x1 to x10 and this is my training data.
and my test data is x11 to x15 right so you are going to pass x11 to it to the function the trained hypothesis function and it will do some sort of prediction for you let's suppose y dash now you know the original label or original value continuous value of x11 right so you are going to check whether y hat is equal to y or not. if it is equal you have predicted perfectly.
if it is not equal then there is some sense of error there is some error right so how do we calculate error we will come back to that in some time but the idea is that you have to find the best hypothesis function that properly maps x to y. so from the training data and with the help of a learning algorithm i am going to sort of build a function which takes input as x right one or more features and do prediction for me right which is a continuous value. prediction
prediction is fine but predicting a continuous value. so what is hypothesis function we will look into a hypothesis function and the gradient descent in next few slides but just hold on. so the hypothesis function f of x or h of x is a mathematical model that represents the relationship between the features and the target. features and the target. features and the target. features and the target. right. the output is the output of the learning function and we want to find out the best f
best f means it should give me minimum error right so whatever y hat i am predicting if
it is equal to y right original value right it is perfect if there is a difference that
difference is a error and we need to sort of build a function or modify a function or
tune the function in such a fashion that y hat actually becomes y that would be the
same idea so now let's come back to the hypothesis function so there can be a linear hypothesis
function what do you mean by linear hypothesis function so assuming right let's suppose this
is a two this is my x and this is my y right so this is my two dimensional data this is x
right and this is y x is age and y is salary right and i have different values if age is
let's suppose 30 the salary is 1 lakh if the age is 35 the salary is 2 lakh if the age is
40 the salary is 3 lakh right this is the data set given to me i have to build a function f
such that if i give let's suppose x value as 45 it should be able to predict y value properly
so let me by looking at the data set let me project this data set into a two dimensional space
one is a dependent variable one is an independent variable right so this is an independent variable
this is a dependent variable now so let me just plot the values here so if the age is 30 age is 40 age
50 and corresponding values of salary right so these are my data points now i want to fit a line on these
data points such that the error is minimized these are my data points coming from my data i want to
fit right fit these data points right with line hyperplane or a plane such that the error is minimized
so let's suppose i fit these data points with this line right with this line so these are my original
data points and i was able to fit the line on this data point right so this is called linear hypothesis
right i want to fit a line so that my error is minimized my error is minimized so when we are talking
about linear hypothesis right when we are talking about linear hypothesis we assume that right the relationship
between x and y is linear that means i can draw a line draw a state line that best describes the data right if it is a linear relationship between x and y i can draw a line that passes through right all the data training data points in that case i can write i can define that line or i can define that line or i can define that line or the function right f of x as mx
x plus c or c plus mx right just just so that it is so c plus mx right so to describe a line we actually can describe it the line using mx plus c right so what is c here c is my theta zero and m is my theta one and x is my sort of independent variable x is my independent variable x is my independent variable now think about it right what is theta zero theta zero is a
intercept what is theta one theta one is the slope right i can draw various lines i could have drawn this line i could have drawn this line i could have drawn this line i could have drawn this line i could have drawn this line i could have drawn this line i can could have drawn this line which line is perfect the line which gives me minimum error how do we calculate error we'll come back that in a minute but which gives me minimum error so you have to draw a line that passes through all the points
to such that the error is minimized so when we are talking about a linear model it is essentially a line which is y equals to mx plus c where i have theta zero and theta one where theta zero is an intercept and theta one is slope right so these theta zero and theta one are parameters and weights and we have to find the best theta zero and theta one that fits the existing data points with minimum error so what is theta zero
theta zero theta zero is essentially y is essentially y intercept right where the line is essentially y intercept right where the line is cutting the y axis y intercept right and theta one is essentially the slope of the line is the slope of the line right so we have to tune both theta one and theta two we have to find the better best value of theta one and theta two so that it fits the training data with minimum error that fits the data with minimum error right so think about it right these are my data points so for
example this was my age this is my salary now I have many points now I can
draw a line like this or I can draw a line like this or I can draw a line like
this or I can draw a function like this I can build a function even like this I
can build many functions which one is the best one which one is the best one
which gives me minimum error how do we calculate error just hold on but which
gives me minimum error so there are two things which we have to look right how
do we define the best line how do we produce the best line best line would be
based on what are the values of theta 0 and theta 1 right now this is when we have
single feature right so in my data set right X contains only one feature and then
I have corresponding Y but many a times house price prediction X might comprises
of many input features right like this X1 X2 X3 and so on so X consists of three
features right in that case right a line might not be sufficient so what we have
to do is we have to do multi variant regression what we have to do is we have
to build a multi variant regression model so think about this right this was my
original data set house price prediction this is Y now we are saying that house
price is not only dependent on living area it is also dependent on number of bedrooms in my
house it is also dependent on number of bedrooms and then there can be many features so this
comprises of my now complete X and I have to build a mapping function such that f gives me proper Y right relationship between X and Y so now our data set is expanded to include more features right now instead of single feature now I have a vector of features I have many features right now I have a single target Y right this is a single target Y right this is a single target Y
Y and X consists of many dimensions so to solve this problem right now I have to build right I have to handle multiple features to a linear hypothesis for that what we have to do is right we have to introduce more termologies right more variables so theta 0 theta 1 theta 2 theta 3 would be needed right would be needed. So how do we visualize multi variant regression?
So in this situation it would be a response plane it would be a plane so theta 0 theta 0 would be again C this is my slope in one dimension slope in another dimension slope in another dimension and so on right so I can build if I have two features I can build in hyperplane for a higher dimensional data and then these parameters theta 1 theta 0 theta 1 theta 2 are essentially we need to identify to fit
this hyperplane properly on the hyperplane properly on the hyperplane properly on the multi-dimensional data set on the multi-dimensional data set so in regression right we are given many training examples these can be in d dimensional space we are also given right we are also given the map the y value right so we are given x and the corresponding y x can be right many data points and x can also consist of many attributes dimensions features
right now we have to build a function f such that we can predict y right with minimum error right with minimum error right with minimum error so we will discuss the hypothesis function in the next video with how do we calculate error and etc etc with the cost function as we move along so in summary in this video we talked about the basic definition of regression which is a supervised learning regression is a supervised learning task where we are predicting continuous numerical
value value it is different from classification in the sense in classification also we are doing prediction but we are predicting class labels in regression we are predicting in continuous values right in regression we have to build a hypothesis function which maps which finds a relationship between x and y right which is where x is my training data right we will continue in the next video thank you for attending take care bye bye
you
