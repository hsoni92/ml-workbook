model_name: HuggingFaceH4/smollm2-360m
dataset: data/support.jsonl

training:
  output_dir: outputs/smollm2
  num_epochs: 3
  batch_size: 4
  gradient_accumulation_steps: 4
  learning_rate: 2.0e-5
  max_length: 512
  warmup_ratio: 0.1
