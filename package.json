{
  "name": "ml-workbook",
  "version": "0.1.0",
  "private": true,
  "description": "Root-level Node tooling for repo-wide linting (mermaid diagram validation). Python deps live in scripts/pyproject.toml.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate:mermaid": "node scripts/validate-mermaid.js"
  },
  "devDependencies": {
    "@probelabs/maid": "^0.0.29"
  }
}
